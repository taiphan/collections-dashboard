{
  "name": "Run Tests After Task",
  "version": "1.0.0",
  "description": "Run the test suite after completing a spec task to verify nothing is broken",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "npm test -- --run"
  }
}